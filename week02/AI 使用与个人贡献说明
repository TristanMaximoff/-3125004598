1.增添 Repair 类的时间字段
在 Repair 实体类中增加 createTime（创建时间）和 updateTime（更新时间）属性
配合数据库自动填充，方便记录报修单的完整时间线
2.学习并实现标准功能
图片上传：学习 Spring Boot 文件上传的标准写法，使用 MultipartFile 接收文件，生成 UUID 唯一文件名，保存到本地并返回访问 URL
JWT 登录：实现标准的 JWT 登录流程，验证账号密码后生成 token，返回给前端
3.理解并应用核心注解
@Data：Lombok 注解，自动生成 getter/setter、toString 等方法，简化实体类代码
@Autowired：Spring 依赖注入注解，自动注入拦截器、Service 等组件
@Value：读取配置文件中的值，实现配置与代码分离
4.简化图片上传逻辑
将图片上传功能从 Service 层直接放到 Controller 层处理
原因：图片上传只涉及文件存储，不涉及数据库操作，无需经过 Service 层，使代码结构更合理
5.增添 getById 接口
在 Controller、Service、Mapper 三层分别添加 getById 方法，原先点击报修单详情没有对应接口，现在可以通过 /repairs/{id} 获取单条报修单的完整信息
6.优化登录接口返回内容
在 UserController 的登录接口中，除了返回 token，还返回用户完整信息（id、role、dormStatus 等）
好处：前端登录后无需再发一次请求获取用户信息，减少一次网络交互
7.完善登录拦截器
增加 OPTIONS 预检请求放行，解决跨域问题
放行 /upload/** 图片访问接口
设计思路：查看报修单详情时已通过 token 验证，图片作为附件无需重复验证，同时也解决了 <img> 标签无法携带 token 的技术限制
8.改造 UserMapper 的 Update 接口
从原来的全量更新改为动态 SQL 更新
使用 MyBatis 的 <set> 和 <if> 标签，只更新前端传入的非空字段
应用场景：用户绑定宿舍时只更新宿舍信息，修改密码时只更新密码，避免覆盖其他字段
9.根据后端接口，开发了完整的宿舍报修系统前端，技术栈为 Vue 3 + Pinia + Vue Router + Element Plus + Axios，实现了与后端的完整对接。
