查 @GetMapping
增 @PostMapping
改 @PutMapping
删 @DeleteMapping
规范、增加可读性

@PathVariable
用于从URL 路径中提取参数，通过{}占位符匹配值，将路径中的数据直接赋值给同名变量。它只适合传递单个值（如 id、账号），是 RESTful 风格的标准写法，参数以纯文本形式展示在地址中，不使用 JSON 格式。
作为前端的一部分，开源可被查询到，安全性低所以不填敏感信息，传单个简单信息
@RequestParam
用于获取URL 地址？后面的键值对参数，是最常见的查询参数接收方式。它适合传递简单的单个或多个参数，数据同样以明文形式展示在地址栏，主要配合 GET 请求使用，不适合传递敏感信息和复杂对象。
eg.网易云每首歌的地址https://music.163.com/#/song?id=18520488
@RequestBody
用于接收前端传来的 JSON 格式请求体数据，参数不会出现在 URL 地址中，安全性更高。它专门用来接收完整的对象数据（如用户信息、报修详情），Spring 会自动将 JSON 转换为 Java 实体对象，是登录、新增、修改等操作的标准用法。
数据安全，传对象
