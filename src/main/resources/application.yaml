spring:
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/korokingdom?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: root
    password: 535532156
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5000ms

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT配置
jwt:
  secret: korokingdom-game-secret-key-2024-springboot
  expire: 86400000

# AI配置（DeepSeek）
ai:
  api:
    key: sk-8927ecf7db79436e8c3d5da17524ff99
    url: https://api.deepseek.com/v1
    model: deepseek-chat

# 游戏配置
game:
  battle:
    min-action-interval: 300
  level:
    exp-daily-limit: 5000

logging:
  level:
    com.game.korokingdom: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

server:
  port: 8080
  servlet:
    context-path: /api